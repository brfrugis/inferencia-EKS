apiVersion: v1
kind: Service
metadata:
  name: vllm-openai
  namespace: inferencia
  labels:
    app: vllm-openai
spec:
  type: ClusterIP
  selector:
    app: vllm-openai
  ports:
    - name: http
      port: 8000
      targetPort: http
