# Diagramas

- **`arquitetura-inferencia-eks.mmd`** — fonte Mermaid (visão global: GitHub Actions, AWS, EKS, Kubernetes, Coralogix).
- **`arquitetura-inferencia-eks.jpg`** — exporte o `.mmd` para imagem (por exemplo [Mermaid Live Editor](https://mermaid.live) → *Export* → PNG/JPEG) e guarde aqui com este nome para o `README.md` na raiz mostrar a figura.

Diagrama opcional adicional:

- **`pipeline-opentelemetry.mmd`** — fluxo de métricas e traces (vLLM → Collector → Coralogix).
